import styled from "styled-components";

const ButtonBack = styled.button`
  font-size: 0.9rem;
  font-weight: 500;
  text-align: center;
  transition: all 0.2s;

  &:hover,
  &:active {
    color: #8e9290;
  }
`;

export default ButtonBack;
